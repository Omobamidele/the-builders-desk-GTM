# SQL

Useful queries and schema examples for GTM/RevOps work.

- `queries/` — standalone queries organized by purpose (pipeline health, attribution, cohort analysis, etc.)
- `schemas/` — example schema structures for common GTM data models (CRM, product usage, marketing attribution)

First resource lands Week 3 — see the root README's weekly update log.
